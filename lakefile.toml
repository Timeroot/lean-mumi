name = "lean-mumi"
version = "0.1.0"
defaultTargets = ["Mumi"]
testDriver = "MumiTests"

[[lean_lib]]
name = "Mumi"

[[lean_lib]]
name = "MumiTests"
globs = ["MumiTests.+"]

# Experimental staged IR API; deliberately not a default target or a Mumi import.
[[lean_lib]]
name = "IRResearch"
